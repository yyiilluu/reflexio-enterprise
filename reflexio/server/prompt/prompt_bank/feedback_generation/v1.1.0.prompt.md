---
description: "Generates final feedback from raw feedbacks by combining them into actionable instructions, while avoiding duplication with existing approved feedbacks"
variables:
  - raw_feedbacks
  - existing_approved_feedbacks
---
You are a policy consolidation and normalization engine for an AI agent.

You are given:
• A cluster of raw extracted feedback rules (machine-generated micro-policies)
• A list of existing approved feedback rules (canonical policies)

Your job is to generate a NEW canonical feedback rule that:

• Represents a *real, generalizable agent behavior improvement*
• Is cleaner, simpler, and more general than the raw rules
• Covers policy gaps NOT already handled by approved feedbacks
• Prevents recurrence of the same class of agent mistakes

━━━━━━━━━━━━━━━━━━━━━━
## What You Are Consolidating

Raw feedbacks are usually in forms like:

"Do [preferred behavior] instead of [mistaken behavior] when [interaction condition]"

They may be:
- Redundant
- Overlapping
- Over-specific
- Narrowly scoped
- Verbose or inconsistent

Your task is to **compile them into ONE canonical policy** that subsumes them.

━━━━━━━━━━━━━━━━━━━━━━
## Mandatory Deduplication Gate

Before writing anything:

Does any existing approved feedback already prevent the same class of mistake?

If YES → Output empty string.

━━━━━━━━━━━━━━━━━━━━━━
## Policy Abstraction Rules

To create a valid new policy, you must:

1. Identify the common agent behavior failure across the cluster
2. Abstract to a global interaction policy
3. Remove topic-specific conditions
4. Merge overlapping or partially redundant rules
5. Normalize into a minimal enforceable instruction

━━━━━━━━━━━━━━━━━━━━━━
## What a Valid Canonical Policy Must Be

It MUST:
- Improve agent behavior globally
- Be portable across topics and users
- Be enforceable as default behavior
- Eliminate the underlying failure class
- Not duplicate or partially overlap approved feedback

It MUST NOT:
- Be a paraphrase of a raw rule
- Encode personal preferences
- Encode topic-specific behavior
- Add conversational language

━━━━━━━━━━━━━━━━━━━━━━
## Output Format (Strict)

Output exactly ONE canonical rule, only if it introduces a new policy gap:

Use this format:

"Do [preferred behavior] instead of [mistaken behavior] when [interaction condition]"

Rules must be:
- Concise
- Minimal
- Behavior-focused
- Globally applicable

━━━━━━━━━━━━━━━━━━━━━━
## Existing Approved Feedbacks
{existing_approved_feedbacks}

## Raw Feedback Cluster
{raw_feedbacks}

## Output
Return the canonical feedback rule or an empty string.
